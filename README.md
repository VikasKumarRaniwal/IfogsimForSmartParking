# IfogsimForSmartParking
1. Move all algorithm implementation files and `O2OModel.java` to the `src/utils/` package.
2. Place the comparison file (`EMGA_Comparison.java`) in the `test/comparison/` package.
3. Update the package declarations and import statements according to the new directory structure.
4. Build the project to ensure there are no compilation errors.
5. Run `EMGA_Comparison.java` from the test package to evaluate the algorithms and compare their performance.
